{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_response.filePath // .tool_input.file_path // empty' | { read -r f; case \"$f\" in *.rb) err=$(ruby -c \"$f\" 2>&1); rc=$?; if [ \"$rc\" -ne 0 ]; then printf 'ruby -c failed: %s\\n%s\\n' \"$f\" \"$err\"; exit 2; fi;; esac; }"
          }
        ]
      }
    ]
  }
}
